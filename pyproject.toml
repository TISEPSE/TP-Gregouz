[project]
name = "tp-gregouz"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.18.4",
    "flask>=3.1.3",
]

[dependency-groups]
blablabla = []
dev = [
    "ruff>=0.15.5",
    "ty>=0.0.21",
]
